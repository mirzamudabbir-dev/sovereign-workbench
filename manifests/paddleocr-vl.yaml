id: paddleocr-vl
served_model_name: PaddlePaddle/PaddleOCR-VL
base_url: http://127.0.0.1:8004/v1
licence_class: permissive
modalities: [text, image]
max_context: 8192
vram_gb: 2.5
quantization: none
supports_grammar: false
task_affinities: [vision_extraction]
description: >
  OCR and layout extraction model for scanned documents, handwriting, engineering drawings,
  and photos. Use to pull raw text and bounding boxes off a page image before any reasoning
  or drafting step runs on that content. Does not produce structured JSON output.
enabled: true
